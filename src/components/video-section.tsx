import Image from "next/image";
import React from "react";

const VideoSection = () => {
  return (
    <div className="w-full border">
      <img src={"/Video.svg"} alt="video" className=" w-full cursor-pointer" />
    </div>
  );
};

export default VideoSection;
